/* introduction_to_arrays.c
   
   Task: Take a scenic tour of array types in C.

   B. Bird - 2023-10-14
*/

#include <stdio.h>



int main() {

    //An array is a fixed-size sequence of zero or more elements 
    //of a single type. The elements of the array are numbered
    //starting at zero, and the number used to identify each element
    //is usually referred to as an index.

    //Task: Declare an array A of 5 integer elements.

    //Task: Declare an array B of 10 double elements.


    //Task: Fill the array A with the values 6, 10, 17, 116, 111

    //Task: Print out the first and last values in A.


    //Task: Fill the array B with the values 0, 0.1, 0.2, 0.3, ..., 0.9
    //      Ideally, find a way to do this automatically.


    //Task: Print out the first and last values in B.

    //Experiment: What happens if we try to use indices of the arrays A and B
    //            that don't exist?
    return 0;
}
